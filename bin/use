#!/usr/lib/python -tt

import argparse

from use.use import app

if __name__ == '__main__':
    parser = argparse.ArgumentParser()
    parser.add_argument('--debug', action='store_true')
    app.debug = parser.parse_args().debug

    app.run()
